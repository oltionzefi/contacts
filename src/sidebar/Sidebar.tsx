import React from 'react';
import { Trans } from '@lingui/macro';

export const Sidebar: React.FC = () => {
	return (
		<div>
			<Trans id="contacts.sidebar.placeholder">Sidebar</Trans>
		</div>
	);
};
